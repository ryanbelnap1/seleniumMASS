# After cloning the repository, copy and paste this entire command into your terminal:

bash -c 'chmod +x setup.sh start.sh && ./setup.sh && source venv/bin/activate && if ! pgrep -f "ollama serve" > /dev/null; then ollama serve > /dev/null 2>&1 & fi && sleep 2 && streamlit run main.py'